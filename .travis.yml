language: node_js

node_js:
  - node

install:
  - npm install -g firebase-tools
  - npm install -g bower
  - cd public
  - bower install
  - cd ..

script:
  - echo "Deploying..."
  
after_success:
  - firebase deploy --token $FIREBASE_TOKEN