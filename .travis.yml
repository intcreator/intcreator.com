language: node_js

node_js:
  - node

before_install:
  - npm i -g polymer-cli
  - npm i -g firebase-tools

install:
  - cd public
  - npm install

script:
  - polymer build
  - cd ..
  - firebase depoly --token $FIREBASE_TOKEN
